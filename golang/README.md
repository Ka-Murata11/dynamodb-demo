# テーブル構造

## 入れ子構造

```JSON
    {
        "id": "string"
        "data": {
            "timestamp": {
                "value1": "number",
                "value2": "number"
            }
        }
        "createdAt": "string"
    }
```
